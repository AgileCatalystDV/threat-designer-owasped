# Retro — integratiedag (lokale stack + LM Studio + tooling)

> **Datum:** 2026-04-04  
> **Type:** korte dagretro (virtueel team + LeadPM)  
> **Gerelateerd:** [`retro-hitl-2026-03.md`](retro-hitl-2026-03.md) (HITL-breed), [`sprints.md`](../../sprints.md), commit `d6bc00d` (o.a. add_threats, Sentry Vite, docs).

---

## 1. Context

- Focuste sessie op **integratie**: threat-agent (`add_threats` normalisatie vóór `ToolNode`, abort bij herhaalde schema-fouten), **Sentry** lokaal (`VITE_SENTRY_BASE_URL` → `localhost:8090`), documentatie-sync en push naar `main`.
- **LeadPM:** testen, errors terugkoppelen, nadenken over gedrag, **LM Studio + Qwen** lokaal beschikbaar maken en koppelen aan de stack.
- **Virtuele dev/CoPM:** implementatie, tests (`test/threat_designer/`), docs en repo-consistentie.

**Inschatting:** zware integratiedag (~8 uur gefocust); complexiteit vooral op **grensvlakken** (LLM ↔ tools ↔ opslag ↔ UI), niet op één algoritme.

---

## 2. Wat ging goed — kort

| Thema | Observatie |
|--------|------------|
| **Samenwerking** | Snelle lus: symptoom → log → fix → opnieuw testen; duidelijke rolverdeling (omgeving & oordeel bij LeadPM, code & docs in repo). |
| **Zichtbare output** | Werkende paden beter afgebakend; QA-captures (`docs/qa/…`) ondersteunen reproduceerbaarheid. |
| **Documentatie** | Local stack, Sentry, checklist en agent-doc bijgewerkt — **bron van waarheid** blijft navigeerbaar. |
| **Commit naar main** | Één duidelijke merge-lijn; geen parallelle “shadow”-staat. |

---

## 3. Uitdagingen / geleerd

| # | Punt | Gevolg / vervolg |
|---|------|------------------|
| L1 | **DynamoDB Local `-inMemory`** | Data overleefd **geen** container-stop; MinIO-volume wél. Herstart = opnieuw door de app-flow voor records die alleen in DynamoDB leven. *Optionele verbetering later:* persistente DynamoDB Local (volume + geen `-inMemory`). |
| L2 | **LLM-variance** | Zelfde stack, ander model/gateway → andere tool-call-karakteristiek; “werkend” blijft **per omgeving** valideren. |
| L3 | **Cognitieve load** | Grote integratiedagen zijn intensief — planning: bewust **recovery** of kleinere scope de dag erna. |

---

## 4. Rondje — per persona (stem)

| Persona | Plus | Vraag / volgende keer |
|---------|------|------------------------|
| **LeadPM** | Real-world test; LM Studio + netwerk; snelle feedback op wat stuk is. | Rust inplannen na zulke dagen; expliciet “go” voor persistence-DynamoDB wanneer prioriteit. |
| **CoPM** | Docs en sprintteksten blijven aansluiten op wat er echt gebouwd is. | Eén zin in `sprints.md` bij grote milestones (retro + commit-ref) — blijft H1 uit HITL-retro versterken. |
| **Dev** | Gerichte fixes (`add_threats_tool_args`, `workflow_threats`); tests erbij. | Bij volgende LLM-integratie:zelfde patroon — coerce → valideer → `model_dump` waar ToolNode dat verwacht. |
| **DevOps** | Compose-profiel `full` voor Sentry blijft duidelijk; env gescheiden (`.env.local` vs Vite). | Documenteren *wanneer* `down -v` (MinIO wissen) — in local-stack FAQ. |
| **Sec** | Abort op herhaalde tool-schemafout voorkomt oneindige agent-loops. | Later: threat-review op opslag-persistence (geen secrets in QA-dumps). |
| **QA** | Checklist + QA-md captures maken regressie bespreekbaar. | Handmatige “smoke” na stack-restart blijft nodig tot Playwright landt (Sprint 8). |

---

## 5. Besluit / acties (voorstel)

| ID | Actie | Eigenaar | Status |
|----|--------|----------|--------|
| R1 | Retro vastgelegd (dit document) | CoPM | ✅ |
| R2 | LeadPM: **prioriteit** vastleggen — DynamoDB-persistence vs andere backlog | LeadPM | ⏸ wanneer klaar |
| R3 | Optioneel: korte regel in `sprints.md` onder planning-check → verwijzing naar deze retro | CoPM | 🔄 |

**LeadPM — slot:** erkenning voor de inzet vandaag; het zware werk zat in **integratie en vertrouwen in de lus** — precies waar dit project het moeilijkst is.

---

*Vastgelegd door virtueel team (CoPM-voice) — LeadPM mag aanvullen of bijsturen.*
